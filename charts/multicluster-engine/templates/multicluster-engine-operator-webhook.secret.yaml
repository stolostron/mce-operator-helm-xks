{{- if not (include "multicluster-engine.utils.isOCP" .) }}
apiVersion: v1
data:
  tls.crt: ""
  tls.key: ""
kind: Secret
metadata:
  name: multicluster-engine-operator-webhook
  namespace: '{{ .Release.Namespace }}'
type: kubernetes.io/tls
{{- end }}
